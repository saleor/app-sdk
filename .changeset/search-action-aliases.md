---
"@saleor/app-sdk": minor
---

Added `options.aliases` to the `SEARCH_ACTION` extension manifest type. It lists extra terms the dashboard command palette matches the action against (e.g. `["taxes", "avatax"]`); they are searchable but never displayed.
