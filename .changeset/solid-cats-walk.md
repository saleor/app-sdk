---
"@saleor/app-sdk": minor
---

Increased action timeout to be 10s instead of 1s. In this window some actions can still be handled gracefully, e.g. Redirect action can wait for user to confirm page exit
